{
  "name": "JATE",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "build": "cd client && npm run build",
    "start": "cd server && node server.js && npm run build",
    "install": "cd server && npm install && cd ../client && npm install",
    "client": "cd client && npm start",
    "server": "cd server && nodemon server.js --ignore client/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "if-env": "^1.0.4",
    "webpack": "^5.84.0"
  },
  "devDependencies": {
    "concurrently": "^5.2.0",
    "nodemon": "^2.0.4",
    "webpack-cli": "^5.1.1"
  }
}
